# SatQuery AI - SIH Problem Statement 167
# Agentic Multi-Modal Remote Sensing Framework
#
# Core runtime (required)
numpy>=1.26
pillow>=10.0
scipy>=1.11

# Geospatial (required for CRS validation / GeoTIFF IO / reprojection)
rasterio>=1.3
pyproj>=3.6

# Deep learning / RS adaptation
torch>=2.2
torchvision>=0.17

# Interactive GUI
streamlit>=1.36

# Interactive map + ROI drawing + live result overlays
folium>=0.16
streamlit-folium>=0.20

# Live imagery acquisition (Microsoft Planetary Computer STAC + signed COG reads)
pystac-client>=0.7
planetary-computer>=1.0

# Polygon extraction for GeoJSON region export
shapely>=2.0

# BigEarthNet-14K acquisition pipeline (scripts/materialize_ben14k.py)
pyarrow>=14.0
requests>=2.31
certifi>=2024.2

# Reports
# (PDF export is dependency-free; install reportlab only if you later swap in a richer exporter)
# reportlab>=4.0

# Demo / evaluation utilities
matplotlib>=3.8

# Testing
pytest>=8.0

# Optional: training extras
# peft>=0.11          # only needed if swapping in HF PEFT adapters
# tifffile>=2024.0    # fallback TIFF reader (auto-detected if present)

fastapi
uvicorn[standard]
